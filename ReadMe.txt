For this assignment, we consider a program scenario that finds the cost of different medical tests. We can also have different variation of those tests. The first case is handled by Factory class and the later one is by Decorator class. The factory class is modified accordingly so that it could accommodate additional services added by the decorator class. The main file contains some example scenario which gives the output as intended. 